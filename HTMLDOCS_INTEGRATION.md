# HTMLDocs Integration Guide

## 📋 ภาพรวม

ระบบได้ถูกอัปเกรดให้ใช้ [HTMLDocs](https://htmldocs.com/) สำหรับการสร้างรายงาน PDF และ Excel แทนการใช้ libraries ในฝั่ง client ซึ่งให้ประสิทธิภาพและคุณภาพที่ดีกว่า

## 🚀 ข้อดีของ HTMLDocs

1. **รองรับภาษาไทย 100%** - ไม่ต้องกังวลเรื่อง font หรือการแสดงผล
2. **สร้างไฟล์บน Server** - ลดภาระงานของ Browser
3. **คุณภาพสูง** - PDF และ Excel ที่สวยงามและสม่ำเสมอ
4. **ง่ายต่อการปรับแต่ง** - แก้ไข HTML/CSS ได้เลย
5. **รวดเร็ว** - ไม่ต้อง load libraries ขนาดใหญ่ที่ client

## 📦 ไฟล์ที่สร้างใหม่

### 1. HTMLDocs Service (`src/utils/htmldocsService.ts`)
```typescript
// ฟังก์ชันหลักสำหรับแปลง HTML เป็น PDF/Excel
convertHTMLToDocument(html, options)
generatePDF(html, options)
generateExcel(html)
downloadDocument(blob, filename)
```

### 2. Report Templates (`src/utils/reportTemplates.ts`)
```typescript
// HTML templates สำหรับรายงานแต่ละประเภท
generateStockReportHTML(data)
generateTransactionReportHTML(data, startDate, endDate)
generatePurchaseRequestReportHTML(data)
```

### 3. API Endpoints
สร้าง endpoints ใหม่สำหรับการสร้างรายงาน:

#### รายงานสต็อก
- `GET /api/reports/stock/pdf` - สร้าง PDF รายงานสต็อก
- `GET /api/reports/stock/excel` - สร้าง Excel รายงานสต็อก

#### รายงานการเบิก-จ่าย
- `GET /api/reports/transactions/pdf?startDate=YYYY-MM-DD&endDate=YYYY-MM-DD` - สร้าง PDF รายงานการเบิก-จ่าย

#### รายงานคำขอซื้อ
- `GET /api/reports/purchase-requests/pdf` - สร้าง PDF รายงานคำขอซื้อ

## ⚙️ การติดตั้ง

### 1. ติดตั้ง Dependencies
```bash
npm install axios
```

### 2. ตั้งค่า API Key

1. สมัครสมาชิกที่ [htmldocs.com](https://htmldocs.com/)
2. รับ API key จาก dashboard
3. เพิ่ม API key ใน `.env`:

```env
HTMLDOCS_API_KEY=your_api_key_here
NEXT_PUBLIC_HTMLDOCS_API_KEY=your_api_key_here
```

## 📝 วิธีใช้งาน

### สร้างรายงาน PDF

```typescript
// จากหน้า Reports
const response = await fetch('/api/reports/stock/pdf');
const blob = await response.blob();

// ดาวน์โหลด
const url = window.URL.createObjectURL(blob);
const link = document.createElement('a');
link.href = url;
link.download = 'stock-report.pdf';
link.click();
```

### สร้างรายงาน Excel

```typescript
const response = await fetch('/api/reports/stock/excel');
const blob = await response.blob();
// ... ดาวน์โหลดเหมือน PDF
```

## 🎨 ปรับแต่ง Templates

แก้ไขไฟล์ `src/utils/reportTemplates.ts` เพื่อปรับแต่งรูปแบบรายงาน:

```typescript
export function generateStockReportHTML(data: any[]): string {
  return `
    <!DOCTYPE html>
    <html>
    <head>
      <style>
        /* แก้ไข CSS ได้ตรงนี้ */
        body { font-family: 'Sarabun', sans-serif; }
        table { width: 100%; border-collapse: collapse; }
      </style>
    </head>
    <body>
      <!-- แก้ไข HTML ได้ตรงนี้ -->
    </body>
    </html>
  `;
}
```

## 🔧 การแก้ไขปัญหา

### ปัญหา: รายงานไม่แสดงภาษาไทย
**แก้ไข**: ตรวจสอบว่า HTML template ใช้ font ที่รองรับภาษาไทย (Sarabun, Noto Sans Thai)

### ปัญหา: API ส่งคืน 500 Error
**แก้ไข**: 
1. ตรวจสอบ API key ใน `.env`
2. ตรวจสอบ credit ใน HTMLDocs account
3. ดู error log ที่ terminal

### ปัญหา: PDF/Excel ไม่ดาวน์โหลด
**แก้ไข**: 
1. ตรวจสอบ console log ใน browser
2. ตรวจสอบว่า API endpoint ส่งคืน blob ถูกต้อง

## 📊 ตัวอย่าง Output

### รายงานสต็อก PDF
- หัวกระดาษพร้อมโลโก้
- ตารางข้อมูลสต็อกพร้อมสี status
- footer พร้อมวันที่พิมพ์

### รายงานการเบิก-จ่าย PDF
- แสดงช่วงวันที่
- รายการเบิก-จ่ายพร้อมผู้ทำรายการ
- สรุปจำนวนรายการ

### รายงาน Excel
- ตารางข้อมูลที่แก้ไขได้
- มี header และ styling พื้นฐาน
- นำไปวิเคราะห์ต่อได้

## 🔗 เอกสารอ้างอิง

- [HTMLDocs Documentation](https://htmldocs.com/docs)
- [HTMLDocs API Reference](https://htmldocs.com/docs/api)
- [HTMLDocs Pricing](https://htmldocs.com/pricing)

## 💡 Tips

1. **ทดสอบ HTML Template ก่อน**: ใช้ browser เปิดดู HTML ก่อนส่งไปสร้าง PDF
2. **ใช้ Cache**: พิจารณา cache รายงานที่สร้างบ่อยๆ
3. **Optimize Images**: ใช้รูปขนาดเล็กใน template
4. **Monitor Credits**: ตรวจสอบ API usage เป็นประจำ

## 🎯 Next Steps

- [ ] เพิ่มรายงานประเภทอื่นๆ (รายงานครุภัณฑ์, รายงานการซ่อมบำรุง)
- [ ] เพิ่ม customization options (เลือก columns, filters)
- [ ] เพิ่ม email delivery สำหรับรายงาน
- [ ] สร้าง scheduled reports (รายงานประจำเดือน/ปี)

---

**หมายเหตุ**: HTMLDocs มี free tier สำหรับทดสอบ หากใช้งานจริงควรอัปเกรดเป็น paid plan
